### AuthOptions

Options for the HTTP Authentication.

#### `bearer` (optional)

**TODO**

#### `user` (`string`)

Alias for `username`

#### `username` (`string`)

The username to use.

#### `pass` (`string`)

Alias for `password`

#### `password` (`string`)

The password to use

#### `sendImmediately` (optional)

**TODO**
